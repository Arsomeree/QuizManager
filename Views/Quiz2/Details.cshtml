@model WebApp1.Models.QuizwithQDetaillView
@{ ViewBag.Title = "Details"; }

<div>
  <h4>Quiz2</h4>
  <hr />
  <dl class="row">
    <dt class="col-sm-2">
      @Html.DisplayNameFor(model => model.QuizTitle)
    </dt>
    <dd class="col-sm-10">
      @Html.DisplayFor(model => model.QuizTitle)


    </dd>
  </dl>
</div>

<div>
<dl class="row">
  <dt class="col-sm-2">
    @foreach (var item in Model.Questions) {
  
 
    @Html.DisplayFor(modelItem => item.Q)}
    
    </dt>
  
</dl>
</div>

<div>
  <a asp-action="Edit" asp-route-id="@Model.Quiz2Id">Edit</a> |
  <a asp-action="Index">Back to List</a>
</div>
